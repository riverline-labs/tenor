{
  "name": "@tenor/sdk",
  "version": "0.1.0",
  "description": "TypeScript/JavaScript SDK for the Tenor contract evaluator",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "wasm/"
  ],
  "scripts": {
    "build:wasm": "bash scripts/build-wasm.sh",
    "build": "tsc",
    "test": "vitest run",
    "prepublishOnly": "npm run build:wasm && npm run build"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/riverline/tenor"
  }
}
