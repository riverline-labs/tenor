// Numeric test — decimal_default_rounding_up
// Decimal(10, 2) with default "2.355" rounds to "2.36" using banker's rounding.
// At the midpoint (5), the preceding digit 5 is odd, so we round up.

fact rounded_up {
  type:    Decimal(precision: 10, scale: 2)
  source:  "finance.rounded_up"
  default: "2.355"
}
