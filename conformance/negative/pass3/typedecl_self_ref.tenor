// Negative test — Pass 3 (TypeDecl)
// A named Record type with a field that references its own type.
// §12.2 Pass 3: "Detect TypeDecl reference graph cycles via DFS"
// §4.5: "TypeDecl reference cycles are caught in Pass 3 by DFS."
//
// LineItemRecord.parent has type LineItemRecord — a direct self-reference.
// Cycle: LineItemRecord → LineItemRecord

type LineItemRecord {
  id:     Text(max_length: 64)
  parent: LineItemRecord
}
