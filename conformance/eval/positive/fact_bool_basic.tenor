// Evaluator test: basic bool fact with comparison rule

fact is_active {
  type:   Bool
  source: "account_service.active"
}

rule check_active {
  stratum: 0
  when:    is_active = true
  produce: verdict account_active { payload: Bool = true }
}
