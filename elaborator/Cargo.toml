[package]
name = "tenor-elaborator"
version = "0.1.0"
edition = "2021"
description = "Tenor DSL elaborator and conformance suite runner"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
